# Models

The live viewer uses the MediaPipe Pose Landmarker lite task model:

```text
models\pose_landmarker_lite.task
```

The file is downloaded automatically by `pose_jitter.py live` if it is missing. It is ignored by Git because it is a generated local model asset.
